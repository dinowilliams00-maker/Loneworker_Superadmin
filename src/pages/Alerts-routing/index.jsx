import React from 'react';
import { Box, Typography } from '@mui/material';

const Alertsrouting = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4">Alerts-routing Page</Typography>
      <Typography variant="body1" sx={{ mt: 2 }}>
        This is a placeholder for the Alerts-routing component.
      </Typography>
    </Box>
  );
};

export default Alertsrouting;
