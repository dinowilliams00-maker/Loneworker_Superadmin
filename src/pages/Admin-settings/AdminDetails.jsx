import React from 'react';
import { Box, Typography } from '@mui/material';

const AdminDetails = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4">AdminDetails Page</Typography>
      <Typography variant="body1" sx={{ mt: 2 }}>
        This is a placeholder for the AdminDetails component.
      </Typography>
    </Box>
  );
};

export default AdminDetails;
