import React from 'react';
import { Box, Typography } from '@mui/material';

const Adminsettings = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4">Admin-settings Page</Typography>
      <Typography variant="body1" sx={{ mt: 2 }}>
        This is a placeholder for the Admin-settings component.
      </Typography>
    </Box>
  );
};

export default Adminsettings;
