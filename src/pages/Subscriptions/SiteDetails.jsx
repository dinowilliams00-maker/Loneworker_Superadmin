import React from 'react';
import { Box, Typography } from '@mui/material';

const SiteDetails = () => {
  return (
    <Box sx={{ p: 3 }}>
      <Typography variant="h4">SiteDetails Page</Typography>
      <Typography variant="body1" sx={{ mt: 2 }}>
        This is a placeholder for the SiteDetails component.
      </Typography>
    </Box>
  );
};

export default SiteDetails;
